version: 0.2

phases:
  install:
    commands:
      - echo "ran install"
      - ruby -e 'exit 1'
      - echo "SHOULDNT SEE THIS COMMAND"
  pre_build:
    commands:
      - echo "SHOULDNT SEE THIS PHASE"
  build:
    commands:
      - echo "SHOULDNT SEE THIS PHASE"
  post_build:
    commands:
      - echo "SHOULDNT SEE THIS PHASE"
